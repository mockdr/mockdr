meta {
  name: Get policy (site or group)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/policies
  body: none
  auth: none
}

params:query {
  siteId: "{{site_id}}"
  ~groupId: "{{group_id}}"
}

headers {
  Authorization: ApiToken {{token}}
}
