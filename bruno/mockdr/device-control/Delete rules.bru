meta {
  name: Delete rules
  type: http
  seq: 4
}

delete {
  url: {{s1_base_url}}/device-control
  body: json
  auth: none
}

headers {
  Authorization: ApiToken {{s1_token}}
  Content-Type: application/json
}

body:json {
  {
    "filter": {
      "ids": ["replace-with-rule-id"]
    }
  }
}
